export type updateUserPayload = {
    name ?: string,
    email ?: string,
    phone ?: string,
    dob   ?: string,
    profession ?: string,
    phoneNumber?: string,
    address?:string,
    gender ?: string 
}
